{% extends '::base.html.twig' %}

{% block body -%}
    <a href="{{path('website')}}" class="btn btn-default btn-lg">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span> Back
    </a>

    <h1>Website <small>create</small></h1>

    {{ form(form) }}
</ul>
{% endblock %}
